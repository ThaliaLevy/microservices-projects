server:
  port: 8888
spring:
  application:
    name: configServer-udemyLeandro
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ThaliaLevy/config-server
          default-label: master-branch
          search-paths:
          - 'greeting-service*' 