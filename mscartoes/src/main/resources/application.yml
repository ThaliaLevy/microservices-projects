spring:
 application:
  name: mscartoes
 rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
  
server:
 port: 0

eureka:
 client:
  service-url:
   defaultZone: http://mscurso-eureka-user:teste@localhost:8761/eureka
 instance:
  instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  prefer-ip-address: true #para resolver o problema de dns

mq:
 queues:
  emissao-cartoes: emissao-cartoes